{
	"name": "server",
	"version": "0.0.0",
	"description": "",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"dev": "nodemon --exec 'npm run start' -w src -e ts,json",
		"start": "tsc -p tsconfig.server.json && node --experimental-specifier-resolution=node ./dist/index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"mongoose": "^7.3.0",
		"node-rest-server": "^4.0.0-1"
	},
	"devDependencies": {
		"@types/node": "^20.3.1",
		"nodemon": "^2.0.22",
		"ts-node": "^10.9.1",
		"typescript": "^5.1.3"
	}
}
